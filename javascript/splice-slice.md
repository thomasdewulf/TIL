# Splice vs slice

## Splice

`array.splice` takes 2  arguments: start and end. It removes the elements specified in the range from the array and returns them. This function **changes the original array**.

## Slice

`array.slice` also takes 2 arguments: start and end. The function creates a new array where the specified range is removed. This function **does not change the original array**.
